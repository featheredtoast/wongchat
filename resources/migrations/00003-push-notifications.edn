{:up   ["CREATE TABLE credentials (
id    serial primary key,
public_key VARCHAR(4000),
private_key VARCHAR(4000))"
        "CREATE TABLE subscriptions (
id    serial primary key,
uid VARCHAR(100),
subscription VARCHAR(4000))"]
 :down ["DROP TABLE credentials"
        "DROP TABLE subscriptions"]}
